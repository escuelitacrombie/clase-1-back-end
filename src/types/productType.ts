type Product = {
    id: `${string}-${string}-${string}-${string}-${string}`;
    nombre: string;
    descripcion: string;
    precio: string;
    vendedor: string;
    stock: string;
}

export default Product